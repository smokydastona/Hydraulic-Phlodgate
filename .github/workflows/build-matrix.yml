name: Build and Validate Matrix

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build and Run Tests
        run: ./gradlew :shared:test --info

      - name: Run Multi-Level Modpack Validation Matrix
        run: ./gradlew :shared:test --tests "org.geysermc.hydraulic.compat.validation.*" --info
